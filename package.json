{
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "analyze": "polymer analyze > analysis.json",
    "sync": "browser-sync start --config 'config/bs-config.js'",
    "preserve": "yarn sync & yarn analyze",
    "serve": "polymer serve",
    "prepages": "mkdir pages-dist && cp -r *.html *.js analysis.json bower_components demo test pages-dist/ && cd pages-dist/bower_components && mkdir gen-test && cd ../../ && cp *.html *.js pages-dist/bower_components/gen-test",
    "pages": "gh-pages -d pages-dist",
    "postpages": "rimraf pages-dist",
    "release": "release-it"
  },
  "dependencies": {
    "browser-sync": "^2.18.13",
    "gh-pages": "^1.0.0",
    "release-it": "^3.2.0"
  },
  "version": "0.0.3"
}
